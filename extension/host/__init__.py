"""Blender-owned host bindings that translate Blender state into inward-facing values.

This package may inspect Blender objects supplied by the composition root. It depends on the
host-neutral ABI client, while the ABI client never imports this package or `bpy`.
"""
