# Blender Hydra integration

This package will expose Hydra renderers found in Blender's bundled OpenUSD
installation. Each supported renderer will appear as a Blender render engine.

The current code only discovers the renderers. Shared rendering sessions and
results are not implemented yet.
