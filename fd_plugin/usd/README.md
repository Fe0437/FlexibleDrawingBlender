# Blender OpenUSD integration

This package checks the OpenUSD installation bundled with Blender. It records
the `pxr` location, the USD version, and the available Hydra renderers. It
rejects an OpenUSD installation that did not come from the running Blender.

Mapping drawing data to USD stages is not implemented yet.
